# 产品定位

Oldman 面向两类人：用 Python 开发应用的人，以及每天在 Dashboard 中处理数据的操作人员。

前者需要可补全的接口、完整示例、清楚的资源归属和可控的部署步骤；后者需要稳定的表单、表格、导航、权限与状态反馈。框架优先解决这些重复出现的工作，不把装饰效果或插件数量当作产品目标。

## 技术取舍

1. **后端渲染优先。** Python 处理数据、权限和主要 HTML。TypeScript 增强交互；简单页面不要求专门编写 Page 子类。
2. **只有一套共享 UI。** Admin 与业务 Dashboard 可以有不同的业务页面和权限，但共用 `oldman.web`、`oldman-web` 的组件、DOM、样式和交互协议。
3. **配置有类型，加载有顺序。** 项目只有一个根 Settings 类型；不同服务读取不同 YAML。App 的配置由 App 自己以明确类型提供。
4. **成熟基础上做必要封装。** 使用 Sanic、SQLAlchemy、Alembic、Jinja、WTForms 等现有实现。增加接线和应用能力，不另造一整套等价底层。
5. **从安装后的包使用。** 用户不需要复制框架内部文件，不依赖作者的目录、数据库或工作环境。源码调试与包安装使用同一组公开接口。
6. **按真实需求扩展。** 通用能力提供明确扩展点，业务逻辑保留在 App 或 Page 中。已有 Core NATS 事件/RPC、Taskiq 持久任务各自服务明确场景；不再套一层含糊的总线，也不预先增加表单 schema 引擎或插件框架来覆盖尚未出现的需求。

## 交互与视觉

界面服务于密集、重复的数据操作：层级明确、间距一致、状态可见，错误不能只留在控制台里。

- 颜色、字体、边框、阴影和动效复用共享设计 token，不在单页另起一套视觉规则。
- 动态替换 HTML 时同时清理旧组件和挂载新组件；弹窗是内容容器，不是另一套表单系统。
- 控件有可理解的标签、可见焦点及错误状态；键盘交互、对比度和减少动画偏好属于基础要求。
- 通用中文、英文及其他语言文本进入统一翻译流程，不把界面文案写死在组件内部。
- 当前框架已有的业务与交互行为是修改基准；有意改变行为时明确说明影响并验证。

这里记录产品方向，不代替接口参考，也不表示所有可访问性或生产部署要求已通过认证。具体能力、参数和限制以[正式文档](docs/README.md)为准。
